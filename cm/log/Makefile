# Target name
LIBNAME =

# Directories
OBJDIR = obj
SRCDIR = src
INCDIR = inc
LIBDIR = lib
DEPDIR = .d

include $(PROJBASE)/cm/global.mk
include $(PROJBASE)/cm/filter_code.mk
include $(PROJBASE)/cm/obj_rules.mk

# include 
INC += -I$(INCDIR)

# Libraries
# ifeq ($(PLATFORM), arm)
# LIBS = /usr/local/log4cplusarm/lib/liblog4cplus.a
# else
# LIBS = /usr/local/log4cplusx86/lib/liblog4cplus.a
# endif

# $(shell ar -x $(LIBS))
# $(shell mv *.o $(OBJDIR))

