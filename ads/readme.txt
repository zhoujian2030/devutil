1. 插入排序与选择排序的时间复杂度都为O(n2)，空间复杂度都为O(1)。但前者只有在待排列数组为逆序的极端情况下才为O(n2)，最好的情况为数组本身就已经是有序数组，此时时间复杂度为O(n)。选择排序则无论什么数组都为O(n2)。正常情况下，插入排序比较次数会少于选择排序，但写入次数会高于选择排序，因为在内循环中，前面已排列好的子列中插入元素时可能需要把多个元素逐个后移。因此两中排序的性能还可能跟硬件有关，比如如果写内存速度较慢的系统中，选择排序的性能就可能好于插入排序。
